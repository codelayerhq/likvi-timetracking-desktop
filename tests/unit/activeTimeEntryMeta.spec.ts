import ActiveTimeEntryMeta from "@/components/ActiveTimeEntryMeta.vue";

describe("@components/ActiveTimeEntryMeta.vue", () => {
  it("exports a valid component", () => {
    expect(ActiveTimeEntryMeta).toBeTruthy();
  });
});
