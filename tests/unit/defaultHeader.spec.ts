import DefaultHeader from "@/components/DefaultHeader.vue";

describe("@components/DefaultHeader.vue", () => {
  it("exports a valid component", () => {
    expect(DefaultHeader).toBeTruthy();
  });
});
