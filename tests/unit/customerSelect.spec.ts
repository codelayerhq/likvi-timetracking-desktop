import CustomerSelect from "@/components/CustomerSelect.vue";

describe("@components/CustomerSelect.vue", () => {
  it("exports a valid component", () => {
    expect(CustomerSelect).toBeTruthy();
  });
});
