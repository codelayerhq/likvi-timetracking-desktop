import TimeEntryEntry from "@/components/TimeEntryEntry.vue";

describe("@components/TimeEntryEntry.vue", () => {
  it("exports a valid component", () => {
    expect(TimeEntryEntry).toBeTruthy();
  });
});
