import ProjectSelect from "@/components/ProjectSelect.vue";

describe("@components/ProjectSelect.vue", () => {
  it("exports a valid component", () => {
    expect(ProjectSelect).toBeTruthy();
  });
});
