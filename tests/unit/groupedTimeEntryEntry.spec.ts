import GroupedTimeEntryEntry from "@/components/GroupedTimeEntryEntry.vue";

describe("@components/GroupedTimeEntryEntry.vue", () => {
  it("exports a valid component", () => {
    expect(GroupedTimeEntryEntry).toBeTruthy();
  });
});
